import { Routes } from '@angular/router';
import { ProfileComponent } from '@user/modules/profile/components/profile/profile.component';

export const routes: Routes = [
  { path: '', component: ProfileComponent },
];
