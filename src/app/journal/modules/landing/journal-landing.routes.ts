import { Routes } from '@angular/router';
import { JournalLandingComponent } from '@journal/modules/landing/journal-landing.component';

export const routes: Routes = [{ path: '', component: JournalLandingComponent }];
