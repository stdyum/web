//zod-cli generate

export interface Subject {
  id: string;
  subject: string;
}

export interface Teacher {
  id: string;
  teacher: string;
}

export interface Group {
  id: string;
  group: string;
}

export interface Room {
  id: string;
  room: string;
}
